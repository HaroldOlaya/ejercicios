#include <Arduino.h>
#include <DataCapture.h>

void setup() {
  // Inicializa la comunicación serie a una velocidad de 9600 baudios
  Serial.begin(9600);
  Serial.println("LINEA NUEVA");
    // Envía "Hello, World" por el puerto serie
}

void loop() {
// Espera un segundo
}